{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Launch Program",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args" : ["--did", "echoesoffrance", "--handle" ,"sreyephleung", "--sessionFile", "./session.txt"]
        }
    ]
}